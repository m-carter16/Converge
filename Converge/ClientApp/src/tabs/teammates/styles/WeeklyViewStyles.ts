import { makeStyles } from "@fluentui/react-theme-provider";

const WeeklyViewStyles = makeStyles(() => ({
  Container: {
    marginLeft: "37px",
    marginTop: 8,
    display: "flex",
    alignItems: "center",
  },
}));

export default WeeklyViewStyles;
