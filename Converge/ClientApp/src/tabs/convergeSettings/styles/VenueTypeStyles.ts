import { makeStyles } from "@fluentui/react-theme-provider";

const VenueTypeStyles = makeStyles(() => ({
  workspaces: {
    "& .ui-checkbox__label": {
      fontWeight: "bold",
    },
  },
}));

export default VenueTypeStyles;
